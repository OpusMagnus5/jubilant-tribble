import { Routes } from '@angular/router';

export const GET_CURRENCIES_PATH: string = "currencies/get-current"
export const REQUESTS_PATH: string = "currencies/requests"

export const routes: Routes = [

];
